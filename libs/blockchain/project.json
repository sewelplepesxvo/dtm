{
  "name": "blockchain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/blockchain/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project blockchain --web",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{projectRoot}/../../dist/libs/blockchain"],
      "options": {},
      "defaultConfiguration": "production",
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    }
  }
}
